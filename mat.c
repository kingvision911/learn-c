#include <math.h>
#include <stdio.h>
	double findSQRT(double N) {return sqrt(N); }

	//Drive code
	
	int main() {
		//given number
		int N = 12;
		
		// function call

		printf("%f ", findSQRT(N));
		return 0;
	}
